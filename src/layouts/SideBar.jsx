import { Stack } from "@mui/material";
import React from "react";

const SideBar = () => {
  return (
    <Stack sx={{ backgroundColor: "blue", maxWidth: "40px", height: "100vh" }}>
      edfg
    </Stack>
  );
};

export default SideBar;
